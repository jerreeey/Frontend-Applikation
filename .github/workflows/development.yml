name: Development Workflow

on:
  push:
    branches: [development] # is triggered if push event in development branch occurs

jobs:
  test_and_lint:
    uses: jerreeey/Frontend-Applikation/.github/workflows/testing_and_linting.yaml@development
